#include <stdio.h>
int main()
{
    int x;
    scanf("%d",&x);
    if (x==1 || x==0)
    {
        printf("Today, I ate %d apple.",x);
    }
    else if (x>1)
    {
        printf("Today, I ate %d apples.",x);
    }
    






    return 0;
}