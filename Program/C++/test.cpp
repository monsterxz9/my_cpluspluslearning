#include <bits/stdc++.h>

using namespace std;
#define ll long long
const int N = 2e5 + 5;

int  reverse90(char *a){

}
int main() {
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
   int n;cin>>n;
   char a[n][n];
   for (int i=0;i<n;i++)
   {
       for (int j = 0; j < n; ++j) {
           cin>>a[i][j];
       }
   }

    return 0;
}